#include <iostream>
#include <iomanip>
#include <string>

using namespace std;

int main()
{
	int vendas;
	int vendas;
	int x;
	vendas1 = 1;
	
	
	vendas = 0; //atribui retorna 1
	x =  (vendas == 1); //compara retorna true ou false 		1 ou 0
	if x==0 
	else x==1
	
	
    string vendedor;
    double salario,vendas;
    cin >> vendedor;
    cout << fixed << setprecision(2);
    cin >> salario >> vendas;
    cout << "TOTAL = R$ " << fixed << setprecision(2) << salario+vendas*0.15 << endl;

    return 0;
}
